// Функція для закриття верхньої полоски
function hideTopBar() {
    const topBar = document.getElementById('top-bar');
    topBar.style.height = '0'; // Змінюємо висоту на 0
    topBar.style.padding = '0'; // Змінюємо padding на 0
}
